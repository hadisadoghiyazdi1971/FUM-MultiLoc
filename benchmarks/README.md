# Benchmarks

Task-specific benchmark tracks are separated by input modality. Each stable track should eventually specify inputs, outputs, splits, metrics, permitted external information, and evaluator version.
