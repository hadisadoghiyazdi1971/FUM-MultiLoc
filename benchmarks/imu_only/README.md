# Imu only Benchmark

Placeholder for the FUM-MultiLoc **imu only** benchmark definition.

Before activation, this folder must specify:

- supported benchmark version;
- exact input fields;
- target outputs;
- split manifest;
- evaluation metrics;
- missing-data policy;
- baseline result provenance.
