# Vision only Benchmark

Placeholder for the FUM-MultiLoc **vision only** benchmark definition.

Before activation, this folder must specify:

- supported benchmark version;
- exact input fields;
- target outputs;
- split manifest;
- evaluation metrics;
- missing-data policy;
- baseline result provenance.
