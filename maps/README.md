# Maps

This folder is reserved for public/sanitized research maps, transformations, and map metadata.

Do not commit unreviewed operational maps or maps containing personal room names. The final release should document pixel-to-metric transformations and the coordinate-system version.

Large raster/vector assets can be distributed with the dataset release and referenced from a manifest rather than stored in Git.
