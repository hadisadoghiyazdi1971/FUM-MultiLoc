# Code

The code tree will contain reference software required to use the benchmark:

- `loader/` — loading/indexing API;
- `evaluation/` — official metrics/evaluator;
- `baselines/` — transparent reference baselines.

No model implementation is claimed in v0.1.0.
