# IMU Modality

This folder documents the inertial component of FUM-MultiLoc.

The final release should explicitly state sensor axes, device coordinate frame, units, sampling rate behavior, calibration, missing-sample handling, and timestamp source. Raw data should be distributed through the dataset host.

See `../schemas/imu.schema.json`.
