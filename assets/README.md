# Assets

Reserved for small repository presentation assets such as a logo or schematic. Do not use this folder for the raw image dataset.

Because image extensions are ignored globally, approved small README images must be added deliberately with `git add -f` after privacy review.
