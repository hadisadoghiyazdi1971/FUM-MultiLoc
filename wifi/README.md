# Wi-Fi Modality

Future public Wi-Fi observations belong to the versioned dataset release, not directly in Git history.

Planned scientific fields include synchronized timestamp, stable public AP identifier, RSSI, frequency, and channel when available. Raw identifiers must be reviewed before release.

See `../schemas/wifi.schema.json` and `../docs/PRIVACY_AND_ANONYMIZATION.md`.
