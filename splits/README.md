# Benchmark Splits

Fixed split definitions will live here once the data inventory and leakage analysis are complete.

Do not generate final splits by randomizing individual highly correlated samples. See `../docs/SPLIT_POLICY.md`.
