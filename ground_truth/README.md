# Ground Truth

Ground truth is a first-class part of the benchmark and must be versioned with the dataset.

Subfolders:

- `reference_points/` — discrete surveyed/defined points.
- `trajectories/` — time-aligned continuous/semi-continuous paths.

See `../docs/GROUND_TRUTH_SPEC.md`.
