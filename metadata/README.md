# Metadata

This folder will contain lightweight metadata tables that describe the released data. In v0.1.0 the CSV files contain headers only and therefore disclose no research observations.

Planned tables:

- `sessions.csv`
- `devices.csv`
- `sensors.csv`
- `locations.csv`

The final data release may use Parquet for large tables; those large files should be distributed outside Git.
